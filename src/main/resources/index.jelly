<!--
  This view is used to render the plugin list page.
-->
<div>
  Adds a new parameter type called Random String Parameter which inserts a random string 
  that prevents Jenkins from merging multiple job runs into one.
</div>
