Adds a new parameter type called Random String Parameter which inserts
a random string that prevents Jenkins from merging multiple job runs 
into one.

See http://wiki.jenkins-ci.org/display/JENKINS/Random+String+Parameter+Plugin
